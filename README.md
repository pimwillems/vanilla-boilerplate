# Pim's Vanilla Boilerplate
Based on HTML5 Boilerplate, packed with Parcel. SCSS enabled.

## Instructions
`npm run dev` to start Parcel server
`npm run build` to build /dist folder that will generate a deployable, static website.
